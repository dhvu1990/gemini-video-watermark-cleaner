{
  "name": "gemini-video-watermark-cleaner",
  "version": "1.0.4",
  "private": false,
  "type": "module",
  "description": "Local-first browser tool for cleaning visible Gemini/Veo video watermark overlays with multi-frame detection and inverse alpha restoration.",
  "scripts": {
    "setup:alpha": "node scripts/sync-alpha-assets.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/main.js && node --check src/video/alpha.js && node --check src/video/catalog.js && node --check src/video/detect.js && node --check src/video/restore.js && node --check src/video/engine.js && node --check src/video/worker.js"
  },
  "dependencies": {
    "mediabunny": "^1.46.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}

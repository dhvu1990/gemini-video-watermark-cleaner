# v1.0.35 - GitHub Pages publishing restored

Date: 2026-08-17

## Why

The repository was made public again and GitHub Pages was re-enabled as the preferred runtime while cleanup-quality work continues.

## Changes

- Restored `.github/workflows/deploy-pages.yml` for Vite production builds.
- Workflow installs dependencies, syncs pinned alpha assets, builds `dist`, uploads the Pages artifact, and deploys it through the `github-pages` environment.
- Uses `pages: write` and `id-token: write` permissions required by the official Pages deployment actions.
- Bumped package, worker metadata, and visible UI version to v1.0.35.

## Expected public URL

`https://dhvu1990.github.io/gemini-video-watermark-cleaner/`

## Cleanup engine

No cleanup algorithm changes are included in v1.0.35. The latest quality change remains v1.0.34 streak-safe directional consensus texture repair.

# v1.0.12 — Directional Background Reconstruction

Date: 2026-08-14

## Trigger / real-video observation

Regression testing on the same 1080x1920 Gemini/Veo sample showed that v1.0.11 fixed the center over-correction from v1.0.10:

- detection remained stable around 88% confidence;
- detector geometry remained x=864, y=1704, size=72;
- estimated alpha gain was about 1.125;
- calibrated Body gain dropped to about 0.990;
- the selected profile became `96-20260520`;
- shape scale stayed at `1.000`;
- Edge gain reached the search maximum `1.30`;
- Edge boost remained `0.030`;
- residual score was about `13.597`.

The cleaned ROI no longer had the strongly darkened center seen in v1.0.10, but the diamond outline was still clearly visible. Because Edge gain had already reached the upper search bound, continuing to increase alpha correction or blur was no longer the preferred next step.

## Research direction

The current upstream `GargantuaX/gemini-watermark-remover` video cleanup work includes weighted cleanup masks, structure guards, inpainting/texture repair paths, edge-band cleanup and optional denoise backends. This supported moving the local browser implementation from additional alpha correction toward explicit background reconstruction for the remaining residual edge.

## Implementation

Added `applyDirectionalEdgeReconstruction()` to `src/video/restore.js`.

The algorithm is intentionally limited to the residual edge band:

1. Compute the gradient of the calibrated alpha map.
2. Use the alpha gradient as the local normal direction to the watermark outline.
3. Search in both normal directions for low-alpha anchor pixels outside the watermark footprint.
4. Bilinearly sample RGB values from the two anchors.
5. Interpolate the expected background color through the edge location.
6. Reduce the reconstruction strength when the two anchor colors differ strongly, which indicates a real image boundary may cross the watermark region.
7. Add a local luma-gradient structure guard to further protect strong real edges.
8. Limit the repair to low/mid-alpha residual edge pixels and avoid the watermark body/core.

## Pipeline placement

Directional reconstruction is applied after:

- inverse-alpha restoration;
- calibrated body/edge gain;
- local edge polish;
- residual footprint cleanup.

Because `applyEdgePolish()` is shared by both the sampled `ZOOMED CLEANED` preview and the full-video export path, the directional reconstruction is automatically applied consistently to both preview and exported MP4.

## Safety / quality constraints

- no global blur;
- no ML model dependency;
- no reconstruction in high-alpha center pixels;
- both sides of the alpha normal must provide valid low-alpha anchors;
- strong cross-anchor color differences reduce blend strength;
- strong local image edges reduce blend strength;
- the repair remains bounded to the calibrated watermark ROI.

## Tests

Extended `tests/restore.test.mjs` with a synthetic residual-outline regression test. The test creates a known background, injects a dark residual into the low-alpha watermark edge band, applies directional reconstruction and verifies that mean RGB error moves closer to the known original background.

## Versioning

- package version: `1.0.12`;
- visible application badge/title: `v1.0.12`;
- release branch: `agent/v1.0.12`.

## Expected real-video outcome

Compared with v1.0.11, the main expected improvement is reduction of the thin dark/blue diamond outline while retaining the corrected center color and preserving real texture crossing the watermark area.

## Next decision gate

If real-video testing still leaves a visible outline after v1.0.12, the next quality tier should be a wider padded-ROI texture repair/inpaint backend or an optional ML denoise backend. Further unconditional increases to Edge gain or Edge polish should not be the first response because the real sample has already demonstrated the limits of alpha-only correction.

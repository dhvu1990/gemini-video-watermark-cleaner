# v1.0.87 — CI Validation Journal

Date: 2026-08-21

## Pull request
PR #92 — `fix: adaptive alpha underflow cap v1.0.87`

## Run #975 — initial PR head
Result: FAILED at `npm test`.

### What passed
- dependency install
- pinned alpha profile sync
- `npm run check`
- all newly-added adaptive alpha underflow cap tests
- the standalone v1.0.86 dark-underflow recovery test
- the rest of the existing suite except one legacy expectation

### Failure
One pre-v1.0.87 test expected `inverseAlphaRestore()` to always cause enough dark collapse for the secondary `DarkUnderflowGuard` to report `attempted=true` and `accepted=true` on an intentionally over-gained dark synthetic scene.

That expectation became invalid after v1.0.87 because the new adaptive pre-clamp cap prevents the collapse before the secondary guard needs to run. CI showed the secondary guard correctly reported zero remaining underflow pixels and therefore did not attempt recovery.

### Resolution
Updated only the stale test semantics:
- now requires the adaptive cap to be attempted;
- requires capped pixels and prevented black pixels;
- verifies secondary guard diagnostics are still present;
- keeps the dark-core luma safety assertion.

The direct standalone `applyDarkUnderflowGuard()` test remains unchanged, so the fallback guard is still independently verified.

No production thresholds or cleanup strengths were loosened to satisfy CI.

## Run #977 — corrected architecture expectation
Result: SUCCESS.

Passed:
- `npm run check` ✅
- `npm test` ✅
- `npm run build` ✅

## Final-head policy
This journal commit changes the PR head, so a final CI run must pass again on the exact head containing both implementation and journals before merge. Do not merge based only on Run #977.

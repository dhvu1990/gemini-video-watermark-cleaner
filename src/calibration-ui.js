import { APP_VERSION } from './version.js';
import { formatAntiStreakTelemetry } from './antiStreakTelemetryFormat.js';
import './batchRetryFix.js';
function ensureFootprintFields() {
  if (document.getElementById('antiFootprintScore')) return;
  const anchor = document.getElementById('antiStructuredImprovement');
  const card = anchor?.closest('.calibration-card');
  if (!card) return;
  const rowA = document.createElement('div'); rowA.className = 'row triple';
  rowA.innerHTML = '<label>Footprint score<input id="antiFootprintScore" value="-" readonly /></label><label>Footprint raw<input id="antiFootprintRaw" value="-" readonly /></label><label>Footprint coverage<input id="antiFootprintCoverage" value="-" readonly /></label>';
  const rowB = document.createElement('div'); rowB.className = 'row';
  rowB.innerHTML = '<label>Shape-aligned density<input id="antiFootprintShapeDensity" value="-" readonly /></label><label>Continuity discount<input id="antiFootprintContinuity" value="-" readonly /></label>';
  card.append(rowA, rowB);
}
ensureFootprintFields();
const result = document.getElementById('detectResult');
const fields = {
  profile: document.getElementById('calProfile'), shapeScale: document.getElementById('calShape'), edgeGain: document.getElementById('calEdgeGain'), edgeBoost: document.getElementById('calEdgeBoost'), offsetX: document.getElementById('calOffsetX'), offsetY: document.getElementById('calOffsetY'), residualScore: document.getElementById('calResidual'), residualEdge: document.getElementById('calResidualEdge'), residualLow: document.getElementById('calResidualLow'), residualHigh: document.getElementById('calResidualHigh'), finalTotal: document.getElementById('finalResidualTotal'), finalLuma: document.getElementById('finalResidualLuma'), finalChroma: document.getElementById('finalResidualChroma'), finalImprovement: document.getElementById('finalResidualImprovement'), backgroundMode: document.getElementById('backgroundMode'), backgroundComplexity: document.getElementById('backgroundComplexity'), backgroundSurfaceMae: document.getElementById('backgroundSurfaceMae'), backgroundEdgeDensity: document.getElementById('backgroundEdgeDensity'), emptyZoneEligible: document.getElementById('emptyZoneEligible'), emptyZoneAttempted: document.getElementById('emptyZoneAttempted'), emptyZoneAccepted: document.getElementById('emptyZoneAccepted'), emptyZoneCandidateResidual: document.getElementById('emptyZoneCandidateResidual'), emptyZoneImprovement: document.getElementById('emptyZoneImprovement'), structuredAttempted: document.getElementById('structuredAttempted'), structuredAccepted: document.getElementById('structuredAccepted'), structuredCandidateResidual: document.getElementById('structuredCandidateResidual'), antiRiskFlags: document.getElementById('antiRiskFlags'), antiDonorAttempted: document.getElementById('antiDonorAttempted'), antiDonorAccepted: document.getElementById('antiDonorAccepted'), antiDonorReason: document.getElementById('antiDonorReason'), antiDonorGuardedRatio: document.getElementById('antiDonorGuardedRatio'), antiDonorMismatch: document.getElementById('antiDonorMismatch'), antiDonorTotalRatio: document.getElementById('antiDonorTotalRatio'), antiAtlasDonors: document.getElementById('antiAtlasDonors'), antiAtlasConfidence: document.getElementById('antiAtlasConfidence'), antiAtlasSpread: document.getElementById('antiAtlasSpread'), antiStructuredAttempted: document.getElementById('antiStructuredAttempted'), antiStructuredAccepted: document.getElementById('antiStructuredAccepted'), antiStructuredMode: document.getElementById('antiStructuredMode'), antiStructuredBefore: document.getElementById('antiStructuredBefore'), antiStructuredAfter: document.getElementById('antiStructuredAfter'), antiStructuredDensity: document.getElementById('antiStructuredDensity'), antiStructuredImprovement: document.getElementById('antiStructuredImprovement'), antiFootprintScore: document.getElementById('antiFootprintScore'), antiFootprintRaw: document.getElementById('antiFootprintRaw'), antiFootprintCoverage: document.getElementById('antiFootprintCoverage'), antiFootprintShapeDensity: document.getElementById('antiFootprintShapeDensity'), antiFootprintContinuity: document.getElementById('antiFootprintContinuity')
};
function reset() { for (const field of Object.values(fields)) if (field) field.value = '-'; }
function format(value, digits = 3) { return Number.isFinite(value) ? Number(value).toFixed(digits) : '-'; }
function bool(value) { return typeof value === 'boolean' ? (value ? 'YES' : 'NO') : '-'; }
function deriveImprovement(calibration, finalCleanup) { const reported = finalCleanup?.improvement; if (Number.isFinite(reported) && Math.abs(reported) > 1e-6) return reported; const before = Number(calibration?.residualScore); const after = Number(finalCleanup?.after?.total); if (!Number.isFinite(before) || before <= 1e-6 || !Number.isFinite(after)) return null; return Math.max(-1, Math.min(1, (before - after) / before)); }
function syncVisibleVersion() { document.title = `Gemini Video Watermark Cleaner v${APP_VERSION}`; const firstBadge = document.querySelector('.badges span'); if (firstBadge) firstBadge.textContent = `v${APP_VERSION}`; }
function assign(field, value) { if (field) field.value = value; }
function render() {
  if (!result) return;
  try {
    const payload = JSON.parse(result.textContent || '{}'); const calibration = payload?.detection?.calibration; if (!calibration) return reset(); const buckets = calibration.residualBuckets || {};
    fields.profile.value = calibration.profile || '-'; fields.shapeScale.value = format(calibration.shapeScale, 3); fields.edgeGain.value = format(calibration.edgeGain, 2); fields.edgeBoost.value = format(calibration.edgeBoost, 3); fields.offsetX.value = format(calibration.offsetX, 2); fields.offsetY.value = format(calibration.offsetY, 2); fields.residualScore.value = format(calibration.residualScore, 3); fields.residualEdge.value = format(buckets.edge, 3); fields.residualLow.value = format(buckets.lowBody, 3); fields.residualHigh.value = format(buckets.highBody, 3);
    const finalCleanup = payload?.finalCleanup; const after = finalCleanup?.after || {}; if (fields.finalTotal) fields.finalTotal.value = format(after.total, 3); if (fields.finalLuma) fields.finalLuma.value = format(after.luma, 3); if (fields.finalChroma) fields.finalChroma.value = format(after.chroma, 3); const improvement = deriveImprovement(calibration, finalCleanup); if (fields.finalImprovement) fields.finalImprovement.value = Number.isFinite(improvement) ? `${(improvement * 100).toFixed(1)}%` : '-';
    const background = payload?.adaptiveBackground || {}; if (fields.backgroundMode) fields.backgroundMode.value = background.mode || '-'; if (fields.backgroundComplexity) fields.backgroundComplexity.value = format(background.complexity, 3); if (fields.backgroundSurfaceMae) fields.backgroundSurfaceMae.value = format(background.surfaceMae, 3); if (fields.backgroundEdgeDensity) fields.backgroundEdgeDensity.value = format(background.edgeDensity, 3); const hard = background.emptyZoneHard || {}; if (fields.emptyZoneEligible) fields.emptyZoneEligible.value = bool(hard.eligible); if (fields.emptyZoneAttempted) fields.emptyZoneAttempted.value = bool(hard.attempted); if (fields.emptyZoneAccepted) fields.emptyZoneAccepted.value = bool(hard.accepted); if (fields.emptyZoneCandidateResidual) fields.emptyZoneCandidateResidual.value = format(hard.candidateAfter?.total, 3); if (fields.emptyZoneImprovement) fields.emptyZoneImprovement.value = Number.isFinite(hard.improvement) ? `${(hard.improvement * 100).toFixed(1)}%` : '-'; const structured = background.structuredRing || {}; if (fields.structuredAttempted) fields.structuredAttempted.value = bool(structured.attempted); if (fields.structuredAccepted) fields.structuredAccepted.value = structured.accepted ? (structured.acceptedMode === 'micro-salvage' ? 'YES (micro)' : 'YES') : (structured.salvageAttempted ? 'NO (micro tried)' : bool(structured.accepted)); if (fields.structuredCandidateResidual) { const candidate = structured.salvageAttempted ? structured.salvageCandidateAfter?.total : structured.candidateAfter?.total; fields.structuredCandidateResidual.value = format(candidate, 3); }
    const anti = formatAntiStreakTelemetry(payload?.antiStreak); assign(fields.antiRiskFlags, anti.riskFlags); assign(fields.antiDonorAttempted, anti.donorAttempted); assign(fields.antiDonorAccepted, anti.donorAccepted); assign(fields.antiDonorReason, anti.donorReason); assign(fields.antiDonorGuardedRatio, anti.donorGuardedRatio); assign(fields.antiDonorMismatch, anti.donorStructureMismatch); assign(fields.antiDonorTotalRatio, anti.donorTotalRatio); assign(fields.antiAtlasDonors, anti.atlasDonors); assign(fields.antiAtlasConfidence, anti.atlasConfidence); assign(fields.antiAtlasSpread, anti.atlasDonorSpread); assign(fields.antiStructuredAttempted, anti.structuredAttempted); assign(fields.antiStructuredAccepted, anti.structuredAccepted); assign(fields.antiStructuredMode, anti.structuredMode); assign(fields.antiStructuredBefore, anti.structuredBefore); assign(fields.antiStructuredAfter, anti.structuredAfter); assign(fields.antiStructuredDensity, anti.structuredDensity); assign(fields.antiStructuredImprovement, anti.structuredImprovement); assign(fields.antiFootprintScore, anti.footprintScore); assign(fields.antiFootprintRaw, anti.footprintRawScore); assign(fields.antiFootprintCoverage, anti.footprintCoverage); assign(fields.antiFootprintShapeDensity, anti.footprintShapeDensity); assign(fields.antiFootprintContinuity, anti.footprintContinuity);
  } catch { reset(); }
}
syncVisibleVersion(); if (result) { new MutationObserver(render).observe(result, { childList: true, subtree: true, characterData: true }); render(); }
